package com.androdevlinux.satschat.core.model

/**
 * Phase 0 placeholder so the module has a compile unit. Replaced in later phases by the real
 * domain model: Contact, Connection, Queue, RatchetState, Message, PayRequest, Invoice, etc.
 */
internal const val MODULE_MODEL = "core:model"
