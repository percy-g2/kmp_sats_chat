package com.androdevlinux.satschat.feature.wallet

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

/**
 * Phase 0 placeholder. Phase 4 replaces this with balance, send, receive, and the seed
 * create/backup/restore flow UI ("seed restores funds, not chats").
 */
@Composable
fun WalletScreenPlaceholder() {
    Text("wallet")
}
