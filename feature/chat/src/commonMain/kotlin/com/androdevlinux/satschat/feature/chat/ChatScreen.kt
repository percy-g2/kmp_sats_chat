package com.androdevlinux.satschat.feature.chat

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable

/**
 * Phase 0 placeholder. Phase 3 replaces this with the conversation + contact-list UI, invitation
 * QR scan/display, message status, and rotation trigger (presenters tested with FakeTransport).
 */
@Composable
fun ChatScreenPlaceholder() {
    Text("chat")
}
